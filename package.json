{
  "name": "sharewise",
  "version": "1.0.0",
  "private": true,
  "description": "Splitwise-style expense entry that writes into a shared Google Sheet.",
  "scripts": {
    "build": "node build.js"
  }
}
